
# Welcome, I am happy to see you

This is the awaken microservice that takes care of media upload, management and optimizations.

## Image upload `/upload`

Image upload is done through the `/upload` endpoint. This endpoint supports `GET`, `POST`, `PUT`, `DELETE` HTTP actions.

### `POST` action

Use this option to upload images. It returns the image id.